# E2E fixtures

- `valid-face.jpg` — derived from scikit-image's bundled `astronaut` photograph (Eileen Collins, NASA, **public domain**). Regenerate with the snippet in `apps/api/tests/fixtures.py` (`valid_face_bytes`).
- `no-face.jpg` — synthetic gradient + seeded noise (generated, no licence concerns).

No personal or scraped photographs are ever committed — see `test-assets/README.md`.
