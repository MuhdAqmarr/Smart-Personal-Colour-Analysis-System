{
  "name": "@coloursense/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared API contracts (zod schemas + TypeScript types) for the ColourSense /api/v1 surface",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3.2.4"
  }
}
